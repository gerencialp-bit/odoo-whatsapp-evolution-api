<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="whatsapp_contact_config_view_form" model="ir.ui.view">
        <field name="name">whatsapp.contact.config.form</field>
        <field name="model">whatsapp.contact.config</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Contact Rules" create="false" delete="false">
                <sheet>
                    <div class="oe_title">
                        <h1>WhatsApp Contact Rules</h1>
                    </div>
                    <group>
                        <group>
                            <label for="revert_promotion_window_hours"/>
                            <div class="o_row">
                                <field name="revert_promotion_window_hours"/>
                                <span> hours</span>
                            </div>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>